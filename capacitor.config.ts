import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lopezwebdev.tokyotoilet',
  appName: 'Tokyo Toilet',
  webDir: 'dist'
};

export default config;
